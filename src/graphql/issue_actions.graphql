mutation CreateIssue ($repositoryId: ID!, $title: String!, $body: String!) {
  createIssue(
    input: {
      repositoryId: $repositoryId,
      title: $title,
      body: $body
    }
  ) {
    issue {
      number
      body
      url
    }
  }
}

#mutation CreateIssueScrum ($repositoryId: ID!, $title: String!, $body: String!) {
#  createIssue(
#    input: {
#      repositoryId: $repositoryId,
#      title: $title,
#      body: $body
#    }
#  ) {
#    issue {
#      number
#      body
#      url
#    }
#  }
#}